import { Header, Footer } from "../layouts";


const Layout = () => {
    return (
        <>
            <Header />
            <main className="mainContainer">
            </main>
            <Footer />
        </>
    );
};

export default Layout;